using System.Drawing;

namespace RobinMagicConsole
{
  public static class Player
  {
    public static Point PlayerCurrentPosition = new Point(2, 2);
    public static Point PlayerNewPosition = new Point(2, 2);
    public static string PlayerSymbol = "0";
    public static bool PlayerHasKey = false;
  }
}